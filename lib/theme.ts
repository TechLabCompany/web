import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    background: {
      default: "#eeeeee",
      paper: "#eeeeee",
    },
    primary: {
      light: "#eeeeee",
      dark: "#eeeeee",
      main: "#eeeeee",
    },
  },
});

export default theme;
